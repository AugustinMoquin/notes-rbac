# Copy to .env and adjust. Falls back to SQLite if DATABASE_URL is unset.
DATABASE_URL=postgresql+psycopg://notes:notes@localhost:5433/notes
JWT_SECRET=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
